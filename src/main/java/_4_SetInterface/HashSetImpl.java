package _4_SetInterface;

public class HashSetImpl {
    public static void main(String[] args) {
        /*
        //HashSet
        => The alphabetical file drawer -> Implemented by a hash table
        //HashSet performance
        add - O(1)
        remove - O(1)
        contains - O(1)
        //Initial Capacity of HashSet
        Hash load factor = no of elements/number of buckets i.e "x" elements/ 16(default bucket number)
        //ReHashing
        if Hash load factor 12/16 = 0.75, then number of buckets size doubled i.e 32 and elements scatered between buckets and continues the same process on and on
        */
    }
}
