package _3_ListInterface;

public class LinkedListPerformance {
    public static void main(String[] args) {

        /*
        //Performance of LinkedList Methods
        get -> O(n)
        set -> O(n)
        contains -> O(n)
        //LinkedList- add and remove item performance
        remove(0) -> O(1) //remove item
        add(0) -> O(1) //add item
        //LinkedList performance, separate the get position then add and remove
        get -> O(n) //if 50th position of the element in the list
        //then once reach 50th position modifying(add and remove) element will take O(1)
        add - O(1)
        remove - O(1)
        */
    }
}
