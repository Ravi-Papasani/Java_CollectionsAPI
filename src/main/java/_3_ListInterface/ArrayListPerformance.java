package _3_ListInterface;

public class ArrayListPerformance {
    public static void main(String[] args) {

        /*
        //Performance of ArrayList Methods
        get -> O(1) //constant time
        set -> O(1)
        contains -> O(n)
        remove(0) -> O(n)
        add(0) -> O(n)
        //ArrayList always resize if the initial size of array exceeds
        // ArrayList doubles the space when it run out of existing space i.e [4] -> [8]
        ArrayList- add performance
        => Best case - O(1) //adding to the last element
        => Worst case - O(n) //when out of space it has to copy to the resize arraylist
        => Most case - O(1) //If not out of space
        */
    }
}
